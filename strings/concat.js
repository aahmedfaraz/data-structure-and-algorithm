// In JavaScript, the concat method is primarily used with arrays and strings to merge two or more arrays or strings into a single array or string.

// let newString = string1.concat(string2, string3, ...);

const str1 = "Hello, ";
const str2 = "World!";
const combinedString = str1.concat(str2, "... can concat more.");

console.log(combinedString); // Output: "Hello, World!... can concat more."
